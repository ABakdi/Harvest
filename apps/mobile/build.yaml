targets:
  $default:
    builders:
      drift_dev:
        options:
          # Dates are ISO-8601 text, not unix seconds: seconds were
          # enough while the phone was the only writer, and sync gave
          # every row a second one ([[Sync-API]], schema v18).
          store_date_time_values_as_text: true
